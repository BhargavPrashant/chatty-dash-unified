# Server
PORT=5000
NODE_ENV=development

# Database
DB_PATH=./whatsapp.db

# WhatsApp Web session storage
WHATSAPP_SESSION_PATH=./whatsapp-session

# Webhook (fallback)
DEFAULT_WEBHOOK_URL=https://your-default-webhook-endpoint.com/webhook/whatsapp
